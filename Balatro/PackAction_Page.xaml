<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="Balatro.PackAction_Page"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Balatro"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="60*"/>
            <RowDefinition Height="40*"/>
        </Grid.RowDefinitions>

        <GridView Grid.Row="0" ItemsSource="{x:Bind ModifyableCards, Mode=TwoWay}" VerticalAlignment="Center" HorizontalAlignment="Center"
                  ItemClick="CardClick"  Visibility="{x:Bind ShowCardsGrid}">
            
        </GridView>

        <GridView Grid.Row="1" ItemsSource="{x:Bind ConsumablePack, Mode=TwoWay}" VerticalAlignment="Center" HorizontalAlignment="Center"
                  ItemClick="PackItemClick">
        </GridView>
        
        <Border Style="{StaticResource InactiveButtonContainer}">
            <Button Content="Use Card" Command="{x:Bind UseCardCommand}"/>
        </Border>

    </Grid>
</Page>
