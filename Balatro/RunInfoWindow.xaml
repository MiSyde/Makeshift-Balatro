<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="Balatro.RunInfoWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Balatro.Models"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="RunInfoWindow"
    xmlns:converter="using:Balatro.Converter">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid Name="BaseGrid">
        <Grid.Resources>
            <converter:LevelConverter x:Key="Level"/>
            <converter:EnumToStringConverter x:Key="Enum"/>
            <converter:HandDataChipsConverter x:Key="Chips"/>
            <converter:HandDataMultiplierConverter x:Key="Multiplier"/>
        </Grid.Resources>
        
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="64"/>
        </Grid.RowDefinitions>

        <Grid Name="ButtonGrid" Grid.Row="0">
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Top" Margin="182, 54, 182, 85">
                <Button Height="76" Width="237"  Content="Poker Hands" Margin="0, 0, 13, 0" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Click="PokerHands_Click" FontFamily="{StaticResource Pixel}"/>
                <Button Content="Blinds" Width="237" Margin="0, 0, 0, 0" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Click="Blinds_Click" FontFamily="{StaticResource Pixel}"/>
                <Button Content="Vouchers" Width="237" Margin="13, 0, 0, 0" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Click="Vouchers_Click" FontFamily="{StaticResource Pixel}"/>
            </StackPanel>
            
        </Grid>

        <Grid Name="InfoGrid" Grid.Row="1" Visibility="{Binding PokerHandsVisibility, Mode=OneWay}" VerticalAlignment="Stretch" Margin="59, 0, 59, 95" HorizontalAlignment="Stretch">
        <Grid.RowDefinitions>
                <RowDefinition/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>

            <ItemsControl ItemsSource="{Binding Game.Player.HandLevels.Values, Mode=OneWay}" Grid.Row="0" Grid.Column="0">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel Orientation="Vertical"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate x:DataType="x:String">
                        <Grid Height="51.5625" Background="LightGray" Margin="0,0,0,2.8">
                            <TextBlock Text="{Binding Converter={StaticResource Level}}" HorizontalAlignment="Center" FontFamily="{StaticResource Pixel}"/>
                        </Grid>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>

            <ItemsControl ItemsSource="{Binding Game.Player.HandLevels.Keys, Mode=OneWay}" Grid.Row="0" Grid.Column="1">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel Orientation="Vertical"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate x:DataType="x:String">
                        <Grid Height="51.5625" Background="LightGray" Margin="0,0,0,2.8">
                            <TextBlock Text="{Binding Converter={StaticResource Enum}}" HorizontalAlignment="Center" FontFamily="{StaticResource Pixel}"/>
                        </Grid>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>

            <ItemsControl ItemsSource="{Binding Game.Player.HandData.Values, Mode=OneWay}" Grid.Row="0" Grid.Column="2">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel Orientation="Vertical"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate x:DataType="x:String">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition/>
                                <ColumnDefinition/>
                                <ColumnDefinition/>
                            </Grid.ColumnDefinitions>

                            <TextBlock Text="{Binding Converter={StaticResource Chips}}" Grid.Column="0" HorizontalAlignment="Right" FontFamily="{StaticResource Pixel}"/>
                            <TextBlock Text="X" Grid.Column="1" HorizontalAlignment="Center" FontFamily="{StaticResource Pixel}"/>
                            <TextBlock Text="{Binding Converter={StaticResource Multiplier}}" Grid.Column="2" HorizontalAlignment="Left" FontFamily="{StaticResource Pixel}"/>
                        </Grid>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>

            <ItemsControl ItemsSource="{Binding Game.Player.HandTimes.Values, Mode=OneWay}" Grid.Row="0" Grid.Column="3">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <StackPanel Orientation="Vertical"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate x:DataType="x:String">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition/>
                                <ColumnDefinition/>
                            </Grid.ColumnDefinitions>

                            <TextBlock Text="#" Grid.Column="0" HorizontalAlignment="Right" FontFamily="{StaticResource Pixel}"/>
                            <TextBlock Text="{Binding}" Grid.Column="1" HorizontalAlignment="Center" FontFamily="{StaticResource Pixel}"/>
                        </Grid>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </Grid>
        <Button Content="Back" Click="Back_Click" Grid.Row="2" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Margin="19, 0, 19, 19" FontFamily="{StaticResource Pixel}"/>
    </Grid>
</Window>
