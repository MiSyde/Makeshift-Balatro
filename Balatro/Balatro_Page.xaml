<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="Balatro.Balatro_Page"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Balatro"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    xmlns:converter="using:Balatro.Converter"
    xmlns:model="using:Balatro.Models">

    <Page.Resources>
        <converter:EnumToStringConverter x:Key="EnumDescription"/>
        <converter:IntValueEnumKeyDictConverter x:Key="IntValue"/>
        <converter:VisibleOver0 x:Key="Over0"/>
    </Page.Resources>
    
    <Grid Name="BaseGrid">

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Grid Name="InfoGrid" Grid.Column="0">

            <Grid.RowDefinitions>
                <RowDefinition/>
                <RowDefinition/>
                <RowDefinition/>
                <RowDefinition/>
            </Grid.RowDefinitions>

            <Grid Name="NameGrid" Grid.Row="0">
                <Grid.RowDefinitions>
                    <RowDefinition/>
                    <RowDefinition/>
                </Grid.RowDefinitions>

                <TextBlock Name="BlindName" Grid.Row="0" Text="{x:Bind ConvertRoundToBlind(game.Round), Mode=OneWay}"/>

                <Grid Name="InnerNameGrid" Grid.Row="1">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition/>
                        <ColumnDefinition/>
                    </Grid.ColumnDefinitions>

                    <Image Name="BlindImage" Grid.Column="0"/>

                    <TextBlock Name="MinimumScore" Grid.Column="1" Text="{x:Bind game.Threshold, Mode=OneWay}"/>
                </Grid>
            </Grid>

            <Grid Name="ScoreGrid" Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                </Grid.ColumnDefinitions>

                <TextBlock Text="Round score" Grid.Column="0"/>

                <TextBlock Name="CurrentScore" Grid.Column="1" Text="{x:Bind game.Player.TotalChips, Mode=OneWay}"/>
            </Grid>

            <Grid Name="HandInfoGrid" Grid.Row="2">
                <Grid.RowDefinitions>
                    <RowDefinition/>
                    <RowDefinition/>
                </Grid.RowDefinitions>

                <Grid Name="InnerHandInfoGrid" Grid.Row="0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition/>
                        <ColumnDefinition/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Name="HandName" Grid.Column="0" Text="{x:Bind game.Player.HighestHand, Mode=OneWay, Converter={StaticResource EnumDescription}}" 
                               Visibility="{x:Bind game.Player.SelectedCards.Count, Mode=OneWay, Converter={StaticResource Over0}}"/>
                    <TextBlock Name="HandLevel" Grid.Column="1" Text="{x:Bind ConvertEnumDictToIntString(game.Player.HandLevels, game.Player.HighestHand), Mode=OneWay}"
                               Visibility="{x:Bind game.Player.SelectedCards.Count, Mode=OneWay, Converter={StaticResource Over0}}"/>
                </Grid>

                <Grid Name="ScoreCalculationGrid" Grid.Row="1">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition/>
                        <ColumnDefinition/>
                        <ColumnDefinition/>
                    </Grid.ColumnDefinitions>

                    <TextBlock Name="Chips" Grid.Column="0" Text="{x:Bind game.Player.Chips, Mode=OneWay}"/>
                    <TextBlock Text="X" Grid.Column="1"/>
                    <TextBlock Name="Multiplier" Grid.Column="2" Text="{x:Bind game.Player.Multiplier, Mode=OneWay}"/>
                </Grid>
            </Grid>

            <Grid Name="OtherGrid" Grid.Row="3">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                </Grid.ColumnDefinitions>

                <Grid Grid.Column="0">
                    <Grid.RowDefinitions>
                        <RowDefinition/>
                        <RowDefinition/>
                    </Grid.RowDefinitions>

                    <Button Name="RunInfo"  Content="Run Info" Grid.Row="0" Click="Show_RunInfo" VerticalAlignment="Stretch" HorizontalAlignment="Stretch"/>
                    <Button Name="Options" Content="Options" Grid.Row="1" Click="Show_Options" VerticalAlignment="Stretch" HorizontalAlignment="Stretch"/>
                </Grid>

                <Grid Name="OtherInfo" Grid.Column="1">
                    <Grid.RowDefinitions>
                        <RowDefinition/>
                        <RowDefinition/>
                        <RowDefinition/>
                    </Grid.RowDefinitions>

                    <Grid.ColumnDefinitions>
                        <ColumnDefinition/>
                        <ColumnDefinition/>
                    </Grid.ColumnDefinitions>

                    <Grid Name="RemainingHandsGrid"  Grid.Row="0" Grid.Column="0">
                        <Grid.RowDefinitions>
                            <RowDefinition/>
                            <RowDefinition/>
                        </Grid.RowDefinitions>

                        <TextBlock Text="Hands" Grid.Row="0"/>
                        <TextBlock Grid.Row="1" Text="{x:Bind game.Player.RemainingHands, Mode=OneWay}"/>
                    </Grid>
                    <Grid Name="RemainingDiscardsGrid" Grid.Row="0" Grid.Column="1">
                        <Grid.RowDefinitions>
                            <RowDefinition/>
                            <RowDefinition/>
                        </Grid.RowDefinitions>

                        <TextBlock Text="Discards" Grid.Row="0"/>
                        <TextBlock Grid.Row="1" Text="{x:Bind game.Player.Discards, Mode=OneWay}"/>
                    </Grid>

                    <TextBlock Name="Money" Grid.ColumnSpan="2" Grid.Row="1" Grid.Column="0" Text="{x:Bind game.Player.Money, Mode=OneWay}"/>

                    <Grid Name="AnteGrid" Grid.Row="2" Grid.Column="0">
                        <Grid.RowDefinitions>
                            <RowDefinition/>
                            <RowDefinition/>
                        </Grid.RowDefinitions>

                        <TextBlock Text="Ante" Grid.Row="0"/>
                        <TextBlock Grid.Row="1" Text="{x:Bind game.Ante.ToString(), Mode=OneWay}"/>
                    </Grid>

                    <Grid Name="RoundGrid" Grid.Row="2" Grid.Column="1">
                        <Grid.RowDefinitions>
                            <RowDefinition/>
                            <RowDefinition/>
                        </Grid.RowDefinitions>

                        <TextBlock Text="Round" Grid.Row="0"/>
                        <TextBlock Grid.Row="1"/>
                    </Grid>
                </Grid>
            </Grid>
        </Grid>

        <Grid Name="TableGrid" Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition/>
                <RowDefinition/>
                <RowDefinition/>
                <RowDefinition/>
            </Grid.RowDefinitions>

            <Grid Name="TopGrid" Grid.Row="0">
                <Grid.RowDefinitions>
                    <RowDefinition/>
                    <RowDefinition/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                </Grid.ColumnDefinitions>
                
            </Grid>

            <Grid Name="MiddleGrid" Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <GridView ItemsSource="{x:Bind game.Player.Cards, Mode=OneWay}" HorizontalAlignment="Center" VerticalAlignment="Center" ItemClick="Card_Selected" IsItemClickEnabled="True" 
                          SelectionMode="Multiple" IsMultiSelectCheckBoxEnabled="False">
                    <GridView.ItemsPanel>
                        <ItemsPanelTemplate>
                            <ItemsWrapGrid Orientation="Vertical"/>
                        </ItemsPanelTemplate>
                    </GridView.ItemsPanel>
                    <GridView.ItemTemplate>
                        <DataTemplate x:DataType="model:Card">
                            <Image Source="{x:Bind Image}" Height="201" Width="150"/>
                        </DataTemplate>
                    </GridView.ItemTemplate>
                </GridView>
            </Grid>

            <Grid Name="BottomGrid" Grid.Row="2">
                <Grid.RowDefinitions>
                    <RowDefinition/>
                    <RowDefinition/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition/>
                    <ColumnDefinition/>
                </Grid.ColumnDefinitions>
            </Grid>
            
            <Grid Name="ActionGrid" Grid.Row="3">
                <Button Name="PlayCards" Command="{x:Bind game.ConfirmCommand}"/>
                <Button Name="DiscardCards" Command="{x:Bind game.DiscardCommand}"/>
            </Grid>
            
        </Grid>
    </Grid>
</Page>
